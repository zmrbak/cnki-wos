# cnki.py - CNKI 文献合并去重工具 使用说明

## 📖 工具简介

本工具用于自动合并多个中国知网 (CNKI) 导出的文献数据文件，进行智能去重处理，并重新编号后输出为标准格式的 TXT 文件，方便后续文献管理和引用。

**适用场景：**
- 多次检索 CNKI 数据库，产生多个导出文件需要合并
- 需要合并不同检索策略得到的文献结果
- 需要去除重复文献记录
- 需要将文献数据转换为标准 TXT 格式以便导入文献管理软件

---

## 📥 输入要求

### 文件位置
- 工具会自动处理**当前工作目录**下的文件
- 无需修改代码中的路径配置

### 文件格式
- **格式：** `.txt`（CNKI 导出时的默认文本格式）
- **命名规则：** 必须以 `CNKI` 开头
- **示例：** `CNKI-1.txt`, `CNKI-2026.txt`, `CNKI-人工智能.txt`

### 内容格式要求
输入文件中的文献条目需符合以下格式：
```
[1] 作者. 标题[J]. 期刊名, 年份, 卷(期): 页码.
[2] 作者. 标题[D]. 学位授予单位, 年份.
[3] 作者. 标题[C]. 会议名称, 年份.
```

**关键特征：**
- 每条文献以 `[数字]` 开头
- 数字编号用方括号括起来
- 文献条目之间以换行分隔
- 支持期刊论文[J]、学位论文[D]、会议论文[C]等文献类型

---

## ⚙️ 处理流程

工具按以下 5 个步骤自动处理：

### 步骤 1：文件扫描
- 自动查找当前目录下所有符合命名规则的 `.txt` 文件
- 显示找到的文件列表
- 如果未找到任何文件，会提示错误并退出

### 步骤 2：内容解析
- 使用正则表达式提取所有 `[数字]` 开头的文献条目
- 自动清理条目中的多余空白字符
- 保留文献的原始格式和内容

### 步骤 3：智能去重
- 基于文献的**文本内容**（忽略编号）进行去重
- 使用 `OrderedDict` 保证去重后的顺序稳定
- 相同内容的不同编号条目会被合并为一条
- 保留第一次出现的版本

### 步骤 4：重新编号
- 按去重后的顺序重新生成编号
- 编号格式：`[1]`, `[2]`, `[3]`...
- 确保编号连续且唯一

### 步骤 5：保存结果
- 输出为纯文本格式
- 使用 `utf-8` 编码（通用兼容）
- 自动生成带时间戳的文件名

---

## 📤 输出说明

### 输出文件
- **文件名：** `merged-cnki-YYYYMMDD-HHMMSS.txt`
- **位置：** 当前工作目录
- **编码：** UTF-8（无 BOM）
- **格式：** 每条文献一行，带 `[数字]` 编号

### 输出格式示例
```
[1] 张三, 李四. 人工智能在教育中的应用研究[J]. 中国电化教育, 2025, 36(5): 45-52.
[2] 王五. 基于深度学习的个性化学习系统研究[D]. 北京师范大学, 2024.
[3] 赵六, 钱七. AI赋能高等教育的路径与策略[C]. 中国教育技术协会年会, 2026.
```

### 控制台输出示例
```
✅ 找到 2 个文献文件：
   - CNKI-人工智能.txt
   - CNKI-教育技术.txt

🎉 合并去重完成！
📊 去重后总文献数：156
📁 输出文件：merged-cnki-20260529-104152.txt
```

---

## 🚀 使用方法

### 前提条件
- 已安装 Python 3.6 或更高版本
- **无需安装额外依赖**（仅使用 Python 标准库）

### 操作步骤

1. **将 CNKI 导出的文件放入同一目录**
   - 例如：`D:/论文撰写/cnki-os/`
   - 确保文件名以 `CNKI` 开头

2. **打开命令行，切换到该目录**
   ```bash
   cd D:/论文撰写/cnki-os
   ```

3. **运行脚本**
   ```bash
   python cnki.py
   ```

4. **查看输出**
   - 控制台会显示处理进度和统计信息
   - 生成的 TXT 文件保存在当前目录

### 用记事本或 VS Code 打开输出文件
- **记事本：** 可以直接打开，中文显示正常
- **VS Code：** 默认以 UTF-8 编码打开，无需特殊设置
- **其他编辑器：** 如果中文显示乱码，请设置打开编码为 UTF-8

---

## ⚠️ 注意事项

### 文件命名
- 必须以 `CNKI` 开头（大写），否则工具无法识别
- CNKI 导出时通常使用此外缀，建议保留

### 内容格式
- 文献条目必须以 `[数字]` 开头，否则无法被正确解析
- 如果 CNKI 导出时使用了其他格式，可能需要手动调整或预处理

### 去重规则
- 去重是基于文献的**完整文本内容**进行精确匹配
- 如果两条文献的内容完全一致（忽略编号），会被识别为重复
- 如果文献内容有细微差别（如标点符号、空格等），不会被识别为重复

### 编码问题
- 输入文件建议使用 UTF-8 编码
- 如果输入文件是 ANSI 或其他编码，可能会出现乱码
- 可以用记事本打开输入文件，另存为 UTF-8 编码后再处理

### 大文件处理
- 理论上支持无限个输入文件
- 单个文件大小建议不超过 10MB
- 如果文件过大，可能会导致内存不足

---

## 🔧 高级用法

### 修改输出文件位置
如果需要将输出文件保存到指定目录，可以修改代码中的 `output_dir` 参数：
```python
if __name__ == '__main__':
    merge_and_deduplicate_literature(output_dir="D:/论文撰写/合并结果")
```

### 修改文件筛选规则
如果需要处理其他命名规则的文件，可以修改代码中的文件筛选条件：
```python
file_list = [
    f for f in os.listdir(".")
    if f.endswith(".txt") and os.path.isfile(f)
    # 去掉 f.startswith("CNKI") 条件，处理所有 txt 文件
]
```

### 保留原始编号
如果需要保留原始编号（不去重），可以注释掉去重相关的代码：
```python
# 注释掉以下代码
# literature_dict = OrderedDict()
# ...
# for _, text in matches:
#     clean_text = re.sub(r'\s+', ' ', text).strip()
#     if clean_text:
#         literature_dict[clean_text] = text.strip()
```

### 自定义去重规则
如果希望基于标题去重（而非完整内容），可以修改去重逻辑：
```python
# 仅提取标题部分作为去重依据
title = extract_title(text)  # 需要自定义标题提取函数
literature_dict[title] = text.strip()
```

---

## ❓ 常见问题

### Q1：工具提示"未找到任何 CNKI 开头的 txt 文件"？
**A：**
- 检查文件是否在当前目录
- 检查文件名是否以 `CNKI` 开头（注意大写）
- 检查文件扩展名是否为 `.txt`
- 在命令行中运行 `dir CNKI*.txt` 确认文件存在

### Q2：某些文件读取失败？
**A：**
- 文件可能损坏，尝试用记事本打开确认
- 文件可能被其他程序占用，关闭后重试
- 文件编码可能不是 UTF-8，用记事本另存为 UTF-8 编码

### Q3：去重后记录数不对？
**A：**
- 这是正常现象，去重会删除重复记录
- 查看控制台输出的统计信息，了解去重详情
- 如果需要保留所有记录（不去重），可以注释掉去重相关的代码

### Q4：输出的 TXT 文件用记事本打开中文乱码？
**A：**
- 工具输出使用 UTF-8 编码，记事本可能需要特殊设置才能正确显示
- 建议用 VS Code、Notepad++ 等现代编辑器打开
- 或者将输出文件用记事本打开，另存为 ANSI 编码

### Q5：可以处理其他格式的文献文件吗？
**A：**
- 当前版本仅支持 CNKI 导出的 `[数字]` 开头格式
- 如果需要处理其他格式，需要修改代码中的正则表达式解析规则

### Q6：去重不准确，有些明显重复的文献没有被去除？
**A：**
- 去重是基于完整文本内容的精确匹配
- 如果两条文献有细微差别（如标点符号、空格、作者格式等），不会被识别为重复
- 可以在去重前对文本进行预处理（如统一标点符号、去除多余空格等）

---

## 📞 技术支持

如有问题或建议，请联系工具开发者。

---

**版本：** 1.0  
**最后更新：** 2026-05-29  
**开发者：** WorkBuddy AI Assistant  
**依赖：** 仅使用 Python 标准库（无需安装额外包）
